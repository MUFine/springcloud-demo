spring:
  application:
    name: client-gateway
  cloud:
    config:
      profiles: test
      discovery:
        enabled: true
        service-id: config-server
server:
  port: 1000
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:2020/eureka/

